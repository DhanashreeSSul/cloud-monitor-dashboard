version: '3.8'
services:
  backend:
    build: ./backend
    ports: ['5000:5000']
    env_file: .env
    networks: [app-net]

  frontend:
    build: ./frontend
    ports: ['3000:80']
    depends_on: [backend]
    networks: [app-net]

networks:
  app-net:
    driver: bridge